# Add translation label to any PR that modifies files in the translations folder
translation:
- changed-files:
  - regex: 'custom_components/solvis_control/translations/.*'

# Add 'feature' label to any PR where the head branch name starts with `feature` or has a `feature` section in the name
enhancement:
 - head-branch: ['^feature', 'feature']